export const isSavedByReqUser = (reqUserId, post) => {
    console.log(post);
    // for (let user of post) {
    //     if (reqUserId === user.id) {
    //         return true;
    //     }
    // }
    return false;
}